use linked::list::*;


impl<T> List<T> {
  /// Returns a mutable reference to the payload of the list's last node
  pub fn peek_last(&self) -> Option<&T> {
    // self.head.as_ref().map(|node| &node.payload)


    unimplemented!()
  }


} //impl
